// src/features/editor-v2/modes/ViewMode.jsx
import React from 'react';

/**
 * ViewMode - Default view mode (no overlays)
 */
const ViewMode = ({ photo }) => {
  return (
    <div className="mode-view">
      {/* No overlay in view mode */}
    </div>
  );
};

export default ViewMode;
